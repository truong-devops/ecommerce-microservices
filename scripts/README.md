# Scripts

Operational helper scripts for local development and maintenance tasks.

Current scripts are placeholders and should be implemented per service lifecycle.

## Implemented Helpers

- `rollback-user-service-profile-phase-a.sh`: revert the latest `user-service` migration (used for Phase A rollback).
