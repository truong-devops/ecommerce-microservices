{
  "name": "@services/order-service",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "echo 'order-service dev scaffold pending'",
    "build": "echo 'order-service build scaffold pending'",
    "test": "echo 'order-service test scaffold pending'",
    "lint": "echo 'order-service lint scaffold pending'"
  }
}
